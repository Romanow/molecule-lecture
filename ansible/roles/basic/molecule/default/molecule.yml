---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ubuntu
    privileged: true
    env:
      MOLECULE_IMAGE: "${MOLECULE_IMAGE}"
      MOLECULE_VERSION: "${MOLECULE_VERSION}"
provisioner:
  name: ansible
  log: true
verifier:
  name: ansible
lint: |-
  yamllint .
  ansible-lint
